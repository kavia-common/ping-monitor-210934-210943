# Example environment configuration for the React frontend
# 
# For same-origin access (recommended):
# The frontend automatically uses relative paths (/api and /ws) which work with
# the React development proxy (setupProxy.js) or production reverse proxy.
# Leave these variables empty or unset for relative path behavior.
#
# For cross-origin access (advanced):
# Set these to absolute URLs only if backend is on a different origin.

# REST API base URL of the backend (optional, uses /api if not set or not absolute)
# REACT_APP_API_BASE=https://vscode-internal-21289-beta.beta01.cloud.kavia.ai:8080

# Optional alias for API base; if both are set, the app will use REACT_APP_API_BASE first
# REACT_APP_BACKEND_URL=https://vscode-internal-21289-beta.beta01.cloud.kavia.ai:8080

# WebSocket base (optional, uses same-origin ws:// or wss:// if not set)
# REACT_APP_WS_URL=wss://vscode-internal-21289-beta.beta01.cloud.kavia.ai:8080

# Frontend URL for CORS configuration (informational)
REACT_APP_FRONTEND_URL=https://vscode-internal-21289-beta.beta01.cloud.kavia.ai:3000
